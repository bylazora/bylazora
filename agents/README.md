# agents/

The rule packs for AI coding agents (Claude Code, Cursor, Copilot and the
rest) that operate on Bylazora migrations. Give an agent these rules and it
knows the invariants; the gate still disposes of whatever it drafts. The
agent proposes; the gate disposes.
